export const ADD_DATASOURCE = 'ADD_DATASOURCE';

export  default function addDataSourceAction(newDatasource){
    return{
        type : ADD_DATASOURCE,
        payload: {
            datasource : newDatasource.dataSource
        }
    }
        
}