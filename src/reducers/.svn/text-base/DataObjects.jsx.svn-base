const dataObjects = (state = {}, action) => {
    switch (action.type) {
        case 'SET_DATA':
            state[action.payLoad[0]] = action.payLoad[1]
            return state
            default:
            return state
    }
}   

export default dataObjects